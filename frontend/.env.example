VITE_BACKEND_TARGET=http://127.0.0.1:8000
# Optional: set when you want frontend to call an absolute API host directly.
# Leave empty in local development to use the Vite proxy and avoid CORS.
# VITE_API_BASE_URL=
